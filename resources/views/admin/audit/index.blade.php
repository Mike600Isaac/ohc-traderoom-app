@extends('layouts.admin')
@section('title','Audit Log') @section('heading','Administrative Audit Log')
@section('content')
<form method="GET" class="mb-5 flex gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><input name="action" value="{{ request('action') }}" placeholder="Filter action, e.g. member.updated" class="min-w-0 flex-1 rounded-lg border-slate-300"><button class="rounded-lg bg-[#10203d] px-5 text-sm font-black text-white">Filter</button></form>
<div class="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="overflow-x-auto"><table class="min-w-full text-left text-sm"><thead class="bg-slate-50 text-[10px] uppercase tracking-wider text-slate-500"><tr><th class="px-5 py-4">Time</th><th class="px-5 py-4">Actor</th><th class="px-5 py-4">Action</th><th class="px-5 py-4">Summary</th><th class="px-5 py-4">Subject</th><th class="px-5 py-4">IP</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($logs as $log)<tr><td class="px-5 py-4 whitespace-nowrap text-xs">{{ $log->created_at->format('M j, Y g:i A') }}</td><td class="px-5 py-4 text-xs">{{ $log->actor?->email ?? 'System / console' }}</td><td class="px-5 py-4 font-mono text-xs">{{ $log->action }}</td><td class="px-5 py-4 font-bold text-[#10203d]">{{ $log->summary }}</td><td class="px-5 py-4 text-xs">{{ class_basename($log->subject_type) }} {{ $log->subject_id }}</td><td class="px-5 py-4 text-xs">{{ $log->ip_address ?: '—' }}</td></tr>@empty<tr><td colspan="6" class="px-5 py-10 text-center text-slate-500">No audit entries.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $logs->links() }}</div>
@endsection
