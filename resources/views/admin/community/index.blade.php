@extends('layouts.admin')
@section('title','Community') @section('heading','Community Channels')
@section('content')
<div class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_380px]"><section class="space-y-4">@forelse($channels as $channel)<form method="POST" action="{{ route('admin.community.update',$channel) }}" class="grid gap-3 rounded-2xl border border-slate-200 bg-white p-5 shadow-sm">@csrf @method('PATCH')<input name="name" value="{{ $channel->name }}" class="rounded-lg border-slate-300 font-black"><textarea name="description" rows="3" class="rounded-lg border-slate-300">{{ $channel->description }}</textarea><div class="grid grid-cols-2 gap-3"><select name="required_path" class="rounded-lg border-slate-300"><option value="">All paths</option>@foreach(['Foundation','Trader','Investor','Ultimate'] as $path)<option @selected($channel->required_path===$path)>{{ $path }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300">@foreach(['draft','published','archived'] as $status)<option value="{{ $status }}" @selected($channel->status===$status)>{{ ucfirst($status) }}</option>@endforeach</select></div><button class="w-fit rounded-lg border border-slate-300 px-4 py-2 text-sm font-black">Save channel</button></form>@empty<div class="rounded-2xl border border-slate-200 bg-white p-8 text-sm text-slate-500">No community channels.</div>@endforelse<div>{{ $channels->links() }}</div></section><aside class="h-fit rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-xl font-black text-[#10203d]">Create channel</h2><form method="POST" action="{{ route('admin.community.store') }}" class="mt-5 grid gap-4">@csrf<input name="name" placeholder="Channel name" class="rounded-lg border-slate-300"><textarea name="description" rows="4" placeholder="Description" class="rounded-lg border-slate-300"></textarea><select name="required_path" class="rounded-lg border-slate-300"><option value="">Available to all paths</option>@foreach(['Foundation','Trader','Investor','Ultimate'] as $path)<option>{{ $path }}</option>@endforeach</select><select name="status" class="rounded-lg border-slate-300"><option value="draft">Draft</option><option value="published">Published</option></select><button class="rounded-lg bg-teal-700 px-5 py-3 text-sm font-black text-white">Create channel</button></form></aside></div>
@endsection
