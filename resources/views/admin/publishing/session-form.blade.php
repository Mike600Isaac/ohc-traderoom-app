@extends('layouts.admin')
@section('title',$item->exists?'Edit Session':'New Session') @section('heading',$item->exists?'Edit Live Session':'Schedule Live Session')
@section('content')
<form method="POST" action="{{ $item->exists?route('admin.sessions.update',$item):route('admin.sessions.store') }}" class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]">@csrf @if($item->exists)@method('PATCH')@endif
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="grid gap-4 sm:grid-cols-2"><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Title</span><input name="title" value="{{ old('title',$item->title) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Starts at</span><input type="datetime-local" name="starts_at" value="{{ old('starts_at',$item->starts_at?->format('Y-m-d\TH:i')) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Ends at</span><input type="datetime-local" name="ends_at" value="{{ old('ends_at',$item->ends_at?->format('Y-m-d\TH:i')) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Agenda</span><textarea name="agenda" rows="5" class="rounded-lg border-slate-300">{{ old('agenda',$item->agenda) }}</textarea></label><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Post-session recap</span><textarea name="recap" rows="4" class="rounded-lg border-slate-300">{{ old('recap',$item->recap) }}</textarea></label><label class="grid gap-2"><span class="text-xs font-black">Join URL</span><input type="url" name="join_url" value="{{ old('join_url',$item->join_url) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Replay URL</span><input type="url" name="replay_url" value="{{ old('replay_url',$item->replay_url) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Registered count</span><input type="number" min="0" name="registered_count" value="{{ old('registered_count',$item->registered_count) }}" class="rounded-lg border-slate-300"></label></div></section>
@include('admin.publishing.status-panel',['item'=>$item,'label'=>'session'])</form>
@endsection
