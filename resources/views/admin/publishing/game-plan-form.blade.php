@extends('layouts.admin')
@section('title',$item->exists?'Edit Game Plan':'New Game Plan') @section('heading',$item->exists?'Edit Game Plan':'Create Game Plan')
@section('content')
<form method="POST" action="{{ $item->exists?route('admin.game-plans.update',$item):route('admin.game-plans.store') }}" class="grid gap-6 xl:grid-cols-[minmax(0,1fr)_360px]">@csrf @if($item->exists)@method('PATCH')@endif
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div class="grid gap-4 sm:grid-cols-2"><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Title</span><input name="title" value="{{ old('title',$item->title) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Trading date</span><input type="date" name="trading_date" value="{{ old('trading_date',$item->trading_date?->format('Y-m-d') ?? now()->format('Y-m-d')) }}" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Market</span><input name="market" value="{{ old('market',$item->market) }}" placeholder="NDX / QQQ" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Bias</span><input name="bias" value="{{ old('bias',$item->bias) }}" placeholder="Bullish / Neutral / Bearish" class="rounded-lg border-slate-300"></label><label class="grid gap-2"><span class="text-xs font-black">Watchlist</span><input name="watchlist_text" value="{{ old('watchlist_text',implode(', ',$item->watchlist ?? [])) }}" placeholder="QQQ, NQ, AAPL" class="rounded-lg border-slate-300"></label><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Key levels · one per line</span><textarea name="key_levels_text" rows="5" class="rounded-lg border-slate-300">{{ old('key_levels_text',implode("\n",$item->key_levels ?? [])) }}</textarea></label><label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">Invalidation</span><textarea name="invalidation" rows="4" class="rounded-lg border-slate-300">{{ old('invalidation',$item->invalidation) }}</textarea></label>@foreach([['video_url','Video URL'],['pdf_url','PDF URL'],['chart_url','Chart URL']] as [$name,$label])<label class="grid gap-2 sm:col-span-2"><span class="text-xs font-black">{{ $label }}</span><input type="url" name="{{ $name }}" value="{{ old($name,$item->$name) }}" class="rounded-lg border-slate-300"></label>@endforeach</div></section>
@include('admin.publishing.status-panel',['item'=>$item,'label'=>'game plan'])</form>
@endsection
