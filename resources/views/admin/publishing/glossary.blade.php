@extends('layouts.admin')
@section('title','Glossary') @section('heading','Research Glossary')
@section('content')
<div class="grid gap-6 xl:grid-cols-[360px_minmax(0,1fr)]">
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-black text-[#10203d]">Add term</h2><p class="mt-2 text-sm text-slate-500">Definitions published here appear in the member Research Centre.</p><form method="POST" action="{{ route('admin.glossary.store') }}" class="mt-5 grid gap-4">@csrf<label class="grid gap-2"><span class="text-xs font-black">Term</span><input name="term" value="{{ old('term') }}" class="rounded-lg border-slate-300" required></label><label class="grid gap-2"><span class="text-xs font-black">OHC definition</span><textarea name="definition" rows="7" class="rounded-lg border-slate-300" required>{{ old('definition') }}</textarea></label><label class="grid gap-2"><span class="text-xs font-black">Status</span><select name="status" class="rounded-lg border-slate-300"><option value="draft">Draft</option><option value="published">Published</option></select></label><button class="rounded-lg bg-teal-600 px-4 py-3 text-sm font-black text-white">Save term</button></form></section>
<section class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 class="text-lg font-black text-[#10203d]">Glossary terms</h2><div class="mt-5 space-y-4">@forelse($terms as $term)<form method="POST" action="{{ route('admin.glossary.update',$term) }}" class="grid gap-3 rounded-xl border border-slate-200 p-4 sm:grid-cols-[180px_minmax(0,1fr)_120px_auto]">@csrf @method('PATCH')<input name="term" value="{{ $term->term }}" class="rounded-lg border-slate-300 text-sm"><textarea name="definition" rows="3" class="rounded-lg border-slate-300 text-sm">{{ $term->definition }}</textarea><select name="status" class="rounded-lg border-slate-300 text-sm"><option value="draft" @selected($term->status==='draft')>Draft</option><option value="published" @selected($term->status==='published')>Published</option></select><button class="font-black text-teal-700">Update</button></form>@empty<p class="rounded-xl bg-slate-50 p-6 text-sm text-slate-500">No glossary terms have been created.</p>@endforelse</div><div class="mt-5">{{ $terms->links() }}</div></section>
</div>
@endsection
